\* Pre-44528cb shape: hooks not deduped, and replay folds not billing (that fix
\* is #234, which came later). Checks HookOnce alone: the same shape also
\* violates LiveMetersWholeTurn, which breadth-first search reaches first.
SPECIFICATION Spec
CONSTANTS
    K = 2
    MaxDrops = 2
    MaxRedeliveries = 1
    MaxSweeps = 2
    BillReplayed = FALSE
    DedupeHooks = FALSE
    LiveFrozenPrice = TRUE
    SweepEnabled = TRUE
    SharedTenant = FALSE
    SweepChecksOwner = FALSE
    LiveExempt = FALSE
    SweepSkipsExempt = TRUE
INVARIANT TypeOK
INVARIANT HookOnce
