"""Slack channel MCP tool implementations (bot-token + membership checks).

Registration lives in ``tools/channels.py`` which dispatches on
``auth.platform``; this package only holds the Slack implementations.
"""
